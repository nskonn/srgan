data:
  lr_train_dir: "~/Desktop/DIV2K_train_LR_bicubic/X4"
  hr_train_dir: "~/Desktop/DIV2K_train_HR"
  lr_val_dir: "data/val/lr"
  hr_val_dir: "data/val/hr"

training:
  batch_size: 16
  epochs: 3 # лимит изображений для обучения
  limit: 10
  lr: 0.0001
  save_interval: 10
  checkpoint_dir: "checkpoints"

model:
  scale_factor: 4
  device: "cuda"  # auto-detect in code